# ⛔ SUPERSEDED — DO NOT READ AS CURRENT

**Archived 2026-09-19.** Moved here by `git mv` from the repository top level
(`sl4_fire_phoenix/`). Nothing was deleted and no file content was changed — all 172 files are
byte-identical to what they were at the top level, recorded as 172 `R100` renames.

## Why this is archived

**1. Stale.** The nested project tree `soul_land_4_fire_phoenix/` (168 files) stops at
**Chapter 31**. The project's real live edge is **after Chapter 51, `The Cost of Quiet`**.
This repository's own `README.md` already described it as "In progress (edge: Ch31)" — which
was accurate as a description of the copy, and misleading as a description of the project.

**2. Duplicate — with zero unique content.** Every one of the 172 files was verified by git
blob SHA on 2026-09-19 to be **byte-identical** to the copy already archived in
`soul-land-universal-kit` at
`_archive/2026-09-19_stale_sl4_copies_at_ch31/sl4_fire_phoenix_STALE_ch31/`. 172 shared paths,
172 identical, 0 differing, 0 unique to either side. Archiving this copy therefore removes no
content that does not exist elsewhere.

Within the tree, the nested `soul_land_4_fire_phoenix/` (168 files) was also verified to be a
strict byte-identical subset of the kit's *other* stale copy (173 files). So this directory was
the third copy of the same 168 files.

This is SL3's **TWO-COPIES LAW** applying to SL4: *a fact maintained in two places will be wrong
in one of them, and the check reads the other.* SL4 had five copies across this GitHub account;
after 2026-09-19 it has one authoritative tree plus one dated identical snapshot.

## The dangerous file

`SOUL_LAND_4_FIRE_PHOENIX_NEXT_STEPS_FOR_CONTINUATION.md` (2125 bytes) is not merely old, it is
**contradicted by an explicit ban**. It presents as current:

- `Dawnflame Kite … 1,120 years / newborn purple-tier` — the project's
  `foundation/STATUS_PANEL.md` §9 ("Not current / forbidden") bans `Dawnflame 960 or 1,120`
- `Dawn-Iron Phoenix Roc … 2,040 years` — §9 bans `Dawn-Iron 2,480 or 2,040`
- `Yan Shuo'er … remains Rank23/SP156` — true value at the current edge is Rank39/SP962
- `No third ring/soul spirit yet; no Level30; no Purple Flame Eidolon Bird yet; no full Fire
  Phoenix/Ultimate Fire/full wings/true flight` — all have since happened

It also names a dead sandbox path, `/home/user/soul_land_4_fire_phoenix/`, as the "Active
project". An agent trusting its title would have regressed the project twenty chapters and
re-committed two banned values. Left byte-identical (blob `2d4dcc1f`) as the historical record.

The other three loose files (`actual_output_hits.txt`, `marker_hits.txt`, `marker_hits2.txt`)
are scan output from an earlier session — tool artefacts, not project content.

## Where the truth lives

| What | Where |
|---|---|
| **Authoritative project** | GitHub private repository `gm5206663-bit/soul_land_4_fire_phoenix` — 653 files, live edge after Chapter 51 |
| Current public snapshot | `soul-land-universal-kit` → `arena_managed_uploads/2026-09-18_chapter51_managed_snapshot/soul_land_4_fire_phoenix/` |
| **Current next-steps entry point** | `soul-land-universal-kit` → `SOUL_LAND_4_FIRE_PHOENIX_NEXT_STEPS_FOR_CONTINUATION.md` at that repo's root |
| Current state panel | `foundation/STATUS_PANEL.md` in either tree above |
| Five-copies comparison | `soul-land-universal-kit` → `WORKSPACE_MAP_2026-09-19_STORYOS_INDEX.md` |
| Full archive/repair delta | `soul-land-universal-kit` → `WORKSPACE_MAP_2026-09-19_ARCHIVE_AND_REPAIR.md` |

## Do not

- Do not read any file here as current state.
- Do not use the `NEXT_STEPS` file here. Use the root one in `soul-land-universal-kit`.
- Do not copy values, ranks, spirit-power figures, soul-spirit ages, or the live edge from here.
- Do not continue this copy. Continue the private repository.
- Do not delete this directory. It is kept for provenance only.
